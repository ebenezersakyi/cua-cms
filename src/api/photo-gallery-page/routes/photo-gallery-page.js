'use strict';

/**
 * photo-gallery-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::photo-gallery-page.photo-gallery-page');
