'use strict';

/**
 * credit-unions-join-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::credit-unions-join-page.credit-unions-join-page');
