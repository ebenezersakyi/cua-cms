'use strict';

/**
 * contact-cutrac-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-cutrac-page.contact-cutrac-page');
