'use strict';

/**
 * eletter-subscription-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::eletter-subscription-page.eletter-subscription-page');
