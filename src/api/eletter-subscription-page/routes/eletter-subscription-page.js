'use strict';

/**
 * eletter-subscription-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::eletter-subscription-page.eletter-subscription-page');
