'use strict';

/**
 * video-gallery-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::video-gallery-page.video-gallery-page');
