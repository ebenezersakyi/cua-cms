'use strict';

/**
 * video-gallery-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::video-gallery-page.video-gallery-page');
